README:cal
		该代码是以C语言为主要开发语言的一个Mac控制台程序，模拟的是Mac终端Terminal下的cal命令打印日历，输出格式和Mac中的cal命令一样。

1.  项目介绍
	(1). 执行方法：打开Mac终端Terminal，并把该project生成的product，即文件中Cal_demo拖入Terminal，然后输入下方的命令形式，即可
       输出相应月份或者年份的日历。
	(2). 算法思想：本算法没有考虑儒略历和格里历的变化，按照公元元年1月1日是周一为基础进行以后每一天的确定。打印整年日历时：通过某年份之
       前所有年份闰年和非闰年的判断，来确定该年的距公元元年1月1日的天数，从而确定该年份的第一天是星期几，进而可以通过判断该年份是否为
       闰年来确定该年份的每一天是星期几。输出月份时计算方法与此相同。

2.  命令形式：
	1.    cal   该输出命令打印的是当前月份的日历
	2.    cal 年份     例如“cal 2017”该命令打印的是指定年份的所有日历，可以打印的范围为1～9999年
	3     cal 月份 年份    例如“cal 11 2017”，该命令可以打印指定年的某个月份的日历，年份范围为1～9999年，月份的范围为1～12。    
	
